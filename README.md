## Module : 7.4 - Defining Routes

```
> app
  page.js # this show the page
  > dashboard
    page.js # this page show in /dashboard
```

Inside the page, next only get the function which is by default exported.
